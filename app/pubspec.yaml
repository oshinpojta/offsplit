name: offsplit
description: "Offsplit — split bills / expenses, settle on UPI."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.21.0
  drift_flutter: ^0.2.0
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.21.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
